# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5433/survey_db"

# Session Secret (required - at least 32 characters)
SESSION_SECRET="your-secret-at-least-32-characters-long-change-this"

# Survey Password (optional - defaults to "secret" if not set)
SURVEY_PASSWORD="secret"

# Server Port
PORT=4001

# Docker Compose Environment Variables (optional)
POSTGRES_DB=survey_db
POSTGRES_USER=survey_user
POSTGRES_PASSWORD=changeme123
DB_PORT=5433